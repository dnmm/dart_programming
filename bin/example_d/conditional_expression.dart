// ignore_for_file: dead_code

void main() {
  // var isLogin = true;
  // var user = isLogin ? "Sonam" : "Guest";
  // print(user);
  var name = 'soname';
  var user = name ?? 'Guest';
  print(user);
}
